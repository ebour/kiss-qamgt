SELECT distinct status, testplan.[group], testplan.subgroup, testplan.subsubgroup, link from testplan LEFT JOIN subgroups on testplan.subgroup == subgroups.name WHERE testplan.[group] == '%s' AND testplan.subgroup == '%s' AND date in (select max(date) as date from testplan) ORDER BY subgroups.[order] ASC;